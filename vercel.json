{
  "buildCommand": "npm run build",
  "outputDirectory": "web-build",
  "devCommand": "npm run web",
  "framework": "expo",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}