{
  "buildCommand": "npm run build",
  "outputDirectory": "web-build",
  "devCommand": "npm run web",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}